/**
 * FarmFlow's LINE Official Account — the one support channel for every portal.
 *
 * Was inlined in the Admin support page only, which is why the Verifier portal
 * and the login screens had no way to reach a human. Kept here so every
 * "ติดต่อผู้ดูแลระบบ" / "Help Desk" link points at the same place, and changing
 * the OA is one edit.
 */
export const LINE_OA_ID = '@334uvoak'
export const LINE_OA_URL = `https://line.me/R/ti/p/${LINE_OA_ID}`
