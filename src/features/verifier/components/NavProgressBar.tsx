/**
 * Re-export of the shared nav-progress primitive (moved to
 * `@/components/ui/nav-progress` so all three portal shells can share it).
 * Kept here for the existing verifier imports (e.g. the tree inspect page).
 */
export { NavProgressBar, LinkNavProgress } from '@/components/ui/nav-progress'
