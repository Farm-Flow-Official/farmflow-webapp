/** Inline badge shown next to placeholder fields that are not yet in the DB. */
export function MockTag() {
  return (
    <span className="ml-1.5 inline-block rounded border border-amber-200 bg-amber-50 px-1.5 py-0.5 text-[10px] font-medium leading-none text-amber-600">
      mock · รอเพิ่มใน DB
    </span>
  )
}
